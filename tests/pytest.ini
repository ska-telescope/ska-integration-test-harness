[pytest]
markers =
    experiments: unit tests for the experiments module which now
        is missing. By default, they are ignored. We are keeping them
        until we decide where to move them.
    core: unit tests for the generic ITH as a Platform Core
    extensions: unit tests for the ITH as a Platform Common Extensions
    platform: unit tests for the ITH as a Platform (Core + Extensions)
