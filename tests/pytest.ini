[pytest]
markers =
    experiments: unit tests for the experiments module which now
        is missing. By default, they are ignored. We are keeping them
        until we decide where to move them.
    core: unit tests for the core module
